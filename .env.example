# Environment variables for ChinChar app
# Rename this file to .env and fill in your actual credentials

# Google OAuth credentials
# Get these from the Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Flask secret key (used for session management)
# You can generate a random key with: python -c "import os; print(os.urandom(24).hex())"
SECRET_KEY=your_secret_key_here

# Database URL (default is SQLite)
# For production, you might want to use PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost/chinchar
